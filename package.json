{
  "name": "frontend",
  "version": "0.1.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend/ && npm run build --prefix frontend/ && cp -a /app/frontend/build/static/. /app/static/"
  }
}
